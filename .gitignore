# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# pipenv
Pipfile.lock

# poetry
poetry.lock

# PDM
.pdm.toml
__pypackages__/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs, editors, machine-local helpers
.vscode/
.idea/
p310.cmd

# macOS
.DS_Store

# ---------------------------------------------------------------------------
# Run output. None of it is an input to anything, and all of it is regenerated
# by re-running the config that produced it -- which IS tracked. A published
# repository carries what is needed to reproduce a result, not the transcript
# of every attempt (D38).
# ---------------------------------------------------------------------------
*.log
data/
outputs/
logs/
manifests/
Report/
aux_py/

# The bare `data/` pattern above matches at any depth, same gotcha as the
# bare `handout.md` pattern documented in the project's own memory. It was
# silently excluding scripts/report_figures/data/measurements.json since the
# figure scripts were first committed (D-less commit "feat(report-figures)"),
# so a fresh clone could not run make_all.py at all -- every fig_*.py in that
# folder reads this file. Un-ignore it explicitly; it is an input the figure
# scripts need, not run output.
!scripts/report_figures/data/
!scripts/report_figures/data/measurements.json

# Sweep and experiment scratch: scripts/sweep.py writes both of these, and the
# tables they produced are quoted in docs/ with their provenance.
configs/sweep/
configs/bench/

# configs/phase1/ is deliberately NOT ignored: those five configs reproduce the
# Phase 1 A/B table quoted in README.md, and a published repo carries what
# regenerates a result (D38).
#
# docs/phase1/ IS ignored. It is a working research document, not a finding. Its
# reading rules -- the conditions that stop the A/B table being misquoted -- are
# inlined into README.md, which is where they have to survive.
docs/phase1/
docs/sweep/
docs/AUDIT_v3.md
docs/BENDERS_SPEC_v3.md
docs/HANDLER_CENSUS.md
docs/SESSION_TOOLING.md
docs/design.md
docs/BENDERS_CORRECTION_PLAN.md

handout.md

# The live experiment config. It is edited constantly -- by runs, by sweeps, and
# by the parallel external audit working in this same folder -- and none of those
# edits belong in a commit. configs/default.example.yaml is the tracked copy:
#
#     cp configs/default.example.yaml configs/default.yaml
#
configs/default.yaml